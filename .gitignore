# dependências
/node_modules
/.pnp
.pnp.js

# build / cache
/.next
/out
/build
*.tsbuildinfo
next-env.d.ts

# banco de dados local (SQLite)
/data/bagagem.db
/data/bagagem.db-*

# variáveis de ambiente — NUNCA comitar chaves
.env
.env*.local

# logs e SO
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.DS_Store
